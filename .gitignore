# secrets — never commit
.env
.env.*
!.env.example
.mcp.json

# tooling / scratch
.claude/
.superpowers/
.worktrees/

# node (backend/.gitignore also covers these)
node_modules/

# design handoff archive (kept on disk, not in repo)
*.zip

# OS
.DS_Store
Thumbs.db
